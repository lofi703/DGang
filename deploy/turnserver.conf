# DGang TURN/STUN server config (coturn)
# REST API auth: Node server signs short-lived creds with this shared secret

listening-port=3478
fingerprint
use-auth-secret
static-auth-secret=m6IF6PHtj/qKeLkcFgzSpP9qayc+J/s/

# relay (RTP/RTCP) port range
min-port=49160
max-port=49200

# public NAT mapping: external public IP -> local private IP
external-ip=140.245.252.78/192.168.1.148

# security realm
realm=dgang.mooo.com

log-file=/var/log/turnserver.log
simple-log

# TURN over plain UDP/TCP (app uses wss for signaling; TURN can be plain)
no-tls
no-dtls

cli-password=dgangTurnCli2026

proc-user=turnserver
proc-group=turnserver